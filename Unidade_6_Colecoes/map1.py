# This application will divide the list of map for 3

numbers = [3, 6, 9, 12, 15]
# Here will get the list numbers and divide for 3
resultado = list(map(lambda x: x // 3, numbers))  # // it display the integer numbers
# Result the list
print(resultado)
